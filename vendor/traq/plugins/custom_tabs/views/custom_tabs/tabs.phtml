<?php
foreach ($custom_tabs as $tab) {
	if (!in_array($app->user->group_id, explode(',', $tab->groups))) { continue; }
	if ($tab->project_id == 0 or (isset($app->project) and $tab->project_id == $app->project->id)) {
?>
	<li><a href="<?php echo $tab->url; ?>"><?php echo $tab->label; ?></a></li>
<?php
	}
}
?>
